{
  "name": "@procureflow/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ProcureFlow API — Hono on Bun (modular monolith).",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.27",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
